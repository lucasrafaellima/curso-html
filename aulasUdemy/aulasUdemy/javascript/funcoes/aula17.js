(function (win, doc) {
    "use strict"
    let isValid = true
    win.alert("ola mundo")

    console.log("menu ", isValid)

    function iniciar() {
        console.log("iniciar menu")
    }
    iniciar()

}) (window, document)